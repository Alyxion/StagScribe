A floating toast notification popup displayed on a neutral gray background canvas of 400x160 pixels. The toast itself is a white rounded card with a subtle shadow underneath to create a lifted appearance. On the left side, a green circle contains a checkmark icon indicating a success state, followed by a bold title reading "File uploaded successfully" and a lighter subtitle showing the file name and size. A blue "Dismiss" action sits on the right edge, and a thin green progress bar near the bottom of the toast shows an auto-dismiss countdown that is roughly two-thirds complete.